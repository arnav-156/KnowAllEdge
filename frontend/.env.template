# Frontend Environment Configuration Template
# Copy this file to .env.local for local development
# Copy to .env.production for production builds

# ==================== API CONFIGURATION ====================

# Backend API URL
# REQUIRED in production, optional in development (defaults to localhost:5000)
# MUST use HTTPS in production for security
VITE_API_URL=http://localhost:5000/api

# ==================== PRODUCTION EXAMPLE ====================
# VITE_API_URL=https://api.yourdomain.com/api

# ==================== ANALYTICS (Optional) ====================
# Google Analytics tracking ID (if using analytics)
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX

# ==================== FEATURE FLAGS (Optional) ====================
# Enable/disable features
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_REQUEST_SIGNING=false

# ==================== SECURITY NOTES ====================
# 1. Never commit .env.local or .env.production to Git
# 2. Use HTTPS URLs in production (enforced by apiClient)
# 3. Store sensitive values in environment variables
# 4. Different values for dev/staging/production

# ==================== SETUP INSTRUCTIONS ====================
# 1. Copy this file: cp .env.template .env.local
# 2. Update VITE_API_URL to match your backend
# 3. For production: Use .env.production with HTTPS URL
# 4. Restart dev server after changes: npm run dev
