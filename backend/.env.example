# ==================== GOOGLE CLOUD CONFIGURATION ====================

# Google Cloud Project Configuration
PROJECT_NAME=your-gcp-project-id

# Google Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-gemini-api-key

# Google Cloud Access Token (for Imagen2)
# Generate using: gcloud auth application-default print-access-token
ACCESS_TOKEN=your-access-token


# ==================== SECURITY CONFIGURATION ====================

# ✅ Master Password for Encrypted Secrets Storage
# Generate using: python secrets_manager.py generate-password
# This encrypts sensitive data in the .secrets file
SECRETS_MASTER_PASSWORD=your-master-password-here

# ✅ Admin API Key (for administrative endpoints)
# Generate using: python -c "import secrets; print(f'sk_admin_{secrets.token_urlsafe(32)}')"
# Save this key securely - it grants full admin access
ADMIN_API_KEY=sk_admin_your-admin-key-here

# ✅ Admin Email (optional)
ADMIN_EMAIL=admin@yourdomain.com

# ✅ JWT Secret Key (for token generation)
# Auto-generated if not provided, but recommended to set manually in production
JWT_SECRET_KEY=your-jwt-secret-key-here

# ✅ HTTPS Enforcement
# Set to 'true' in production to force HTTPS redirects
# Set to 'false' in development (localhost)
FORCE_HTTPS=false


# ==================== REDIS CONFIGURATION ====================

# Redis connection settings (for caching and distributed sessions)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password-here
REDIS_DB=0


# ==================== APPLICATION CONFIGURATION ====================

# Flask Environment (development, staging, production)
FLASK_ENV=development

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS Origins (comma-separated list of allowed origins)
# Update with your frontend domains in production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Upload folder for temporary files
UPLOAD_FOLDER=uploads


# ==================== RATE LIMITING ====================

# Quota limits per tier (override defaults)
# Format: QUOTA_<TIER>_<METRIC>=<value>
# QUOTA_FREE_RPM=10
# QUOTA_FREE_RPD=100
# QUOTA_BASIC_RPM=15
# QUOTA_PREMIUM_RPM=30


# ==================== PROMETHEUS METRICS ====================

# Prometheus metrics port (if different from main app)
# PROMETHEUS_PORT=9090


# ==================== NOTES ====================
# 
# 1. NEVER commit this file with real credentials to Git
# 2. Copy this file to .env and fill in real values
# 3. For production, use environment-specific secrets management:
#    - AWS Secrets Manager
#    - Azure Key Vault
#    - Google Cloud Secret Manager
#    - HashiCorp Vault
# 4. Use encrypted secrets storage: python secrets_manager.py import-env
# 5. Rotate secrets regularly (especially after team member changes)
